#version 330

uniform sampler2D colorText;
uniform int texturing;

in vec2 ftextcoord;
in vec4 fcolor;

out vec4 fragColor;

void main()
{
	vec4 objectColor = fcolor;

	if (texturing == 1)
	{
		objectColor = objectColor * texture2D(colorText,ftextcoord);
	}	

	fragColor = objectColor;
}

